// Phase 7 — job history screen. Stub.

export function mount(root: HTMLElement): () => void {
  root.innerHTML = `
    <main class="job-main" style="padding-top: 64px; text-align: center; color: var(--text-dim); font-size: 14px;">
      History — coming in Phase 7
    </main>`
  return () => {}
}
